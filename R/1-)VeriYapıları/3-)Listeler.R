

x <- list(1,2,3) # liste oluşturma fonksiyonu
x

y <-list('A','B','C') # karakter listesi
y


z <- list('A',1,2,'B') # karışık liste
z


vektorlistesi <- list(c(1,2,3,4),2,'A') # bu şekilde listenin içerisine vektör ekledik
vektorlistesi

# Listeler içerisinde farklı tiplerde veriler tutabiliriz

dict <- list('a' = c(1,2,3), 'b' = c(4,5,6)) # indislere isim verebiliriz
            # a indisinin değeri bir vektör. b indisinin değeri bir vektör
dict

liste <- list('vektör1'=c(1,2,3),'vektör2'=c(4,5,6))
liste

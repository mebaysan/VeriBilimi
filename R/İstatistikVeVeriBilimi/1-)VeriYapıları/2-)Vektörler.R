x = 5


# <- atama operatörüdür

vektor1 = c(1,2,3,4,5) # vektör oluşturmak için kullanılan fonksiyon

vektor2 <- c(10,20,30,40,50) # fonksiyonun çıktısını vektor2 değişkenine atıyoruz


stringler <- c('B','A','Y','S','A','N') # R'da stringleri " ile ve ' ile belirtebiliriz

karisik <- c('A',2,3,'B') # gönderdiğimiz sayıları da STRING olarak almıştır
karisik

# ! R'da vektörler içerisinde tek tip veriler tutulur !
